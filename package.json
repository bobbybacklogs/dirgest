{
  "name": "dirgest-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm test --workspaces",
    "dev": "node scripts/dev.js",
    "dev:api": "node packages/api/bin/server.js",
    "dev:web": "npm run dev --workspace=packages/web"
  },
  "engines": {
    "node": ">=18"
  }
}
